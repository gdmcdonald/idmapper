% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter-ids.R
\name{filter_by_existing_identities}
\alias{filter_by_existing_identities}
\title{Restrict a dataset to rows matching an existing identity graph}
\usage{
filter_by_existing_identities(df, env_name = "person", verbose = FALSE)
}
\arguments{
\item{df}{A data frame with one or more columns marked via \code{\link[=mark_id]{mark_id()}}.}

\item{env_name}{Entity type name to check against (default \code{"person"}).}

\item{verbose}{If \code{TRUE}, print how many rows were kept.}
}
\value{
\code{df}, filtered to rows with at least one matching identifier for
\code{env_name}. If that entity type's graph is empty, or \code{df} has no
marked columns for it, returns a zero-row slice of \code{df}.
}
\description{
Keeps only the rows of \code{df} that have at least one marked identifier
already present in an entity type's identity graph - without adding any
new identifiers or edges to that graph. Useful for datasets you want to
enrich/cross-check against known identities (e.g. an LDAP extract) but
not treat as a source of new identity links.
}
