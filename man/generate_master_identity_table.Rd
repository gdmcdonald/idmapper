% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/master-table.R
\name{generate_master_identity_table}
\alias{generate_master_identity_table}
\title{Collapse an identity graph into a master identity table}
\usage{
generate_master_identity_table(
  env_name = NULL,
  verbose = TRUE,
  id_prefix = NULL,
  store_in_env = TRUE
)
}
\arguments{
\item{env_name}{Optional entity type name (e.g. \code{"person"}). If \code{NULL}
(the default), a master table is generated for every entity type
currently initialised via \code{\link[=init_id_envs]{init_id_envs()}}, returned as a named list.}

\item{verbose}{If \code{TRUE} (the default), print progress messages.}

\item{id_prefix}{Prefix used for generated master IDs. Defaults to the
upper-cased \code{env_name} followed by \code{"-"} (e.g. \code{"PERSON-"}).}

\item{store_in_env}{If \code{TRUE} (the default), also store the resulting
table so \code{\link[=get_master_table]{get_master_table()}} can retrieve it later without
regenerating it.}
}
\value{
A tibble with one row per master identity: an ID column named
\code{"<env_name>_id"}, and one list-column per \code{id_type} found in the
graph. If \code{env_name} is \code{NULL}, a named list of such tibbles, one per
entity type.
}
\description{
Finds the connected components of an entity type's identity graph (built
up via repeated \code{\link[=add_to_identity_graph]{add_to_identity_graph()}} calls) and collapses each
component into a single master identity, assigned an ID like
\code{"PERSON-1"}, \code{"PERSON-2"}, .... The resulting table has one row per
master identity and one list-column per distinct \code{id_type} seen (e.g.
\code{email}, \code{unikey}), each holding every identifier value that resolved to
that identity.
}
