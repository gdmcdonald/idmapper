% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state.R
\name{init_id_envs}
\alias{init_id_envs}
\title{Initialise identity graph environments}
\usage{
init_id_envs(
  names = c("person", "project", "organisation"),
  suffix = "_env",
  envir = NULL,
  init_graph = TRUE
)
}
\arguments{
\item{names}{Character vector of entity type names to initialise.}

\item{suffix}{Kept only for backwards compatibility with the original
\code{id_map.R} script's argument list; no longer used, since state is now
stored internally rather than as \verb{<name>_env} variables in a target
environment.}

\item{envir}{Kept only for backwards compatibility; ignored. State is
always stored in an internal package environment now, never in
\code{envir}.}

\item{init_graph}{If \code{TRUE} (the default), initialise each entity type
with an empty, undirected graph (\code{\link[igraph:make_empty_graph]{igraph::make_empty_graph()}}).}
}
\value{
Invisibly, \code{NULL}. Called for its side effect of creating (or
resetting) package-internal state for each entity type in \code{names}.
}
\description{
Creates one empty identity graph per named entity type (for example
\code{"person"}, \code{"project"}). Must be called before \code{\link[=mark_id]{mark_id()}} /
\code{\link[=add_to_identity_graph]{add_to_identity_graph()}} for a given entity type, and is safe to call
again later to reset a type back to an empty graph.
}
\examples{
init_id_envs(c("person", "project"))
}
