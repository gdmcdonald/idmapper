% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join-ids.R
\name{join_master_ids}
\alias{join_master_ids}
\title{Attach resolved master IDs onto a dataset}
\usage{
join_master_ids(df, suffix = "_UID", verbose = FALSE)
}
\arguments{
\item{df}{A data frame with one or more columns marked via \code{\link[=mark_id]{mark_id()}}.}

\item{suffix}{Suffix appended to each entity type's name to form its new
ID column name (default \code{"_UID"}, e.g. \code{"person_UID"}).}

\item{verbose}{If \code{TRUE}, print progress messages.}
}
\value{
\code{df} with one additional column per entity type found among its
marked columns (e.g. \code{person_UID}), each holding the row's resolved
master ID, or \code{NA} if none of that row's marked identifiers matched
anything in the master table.
}
\description{
For each entity type represented among \code{df}'s \code{\link[=mark_id]{mark_id()}}-marked columns,
looks up each row's identifier(s) against that entity type's master
identity table (see \code{\link[=generate_master_identity_table]{generate_master_identity_table()}}) and attaches the
resolved master ID as a new column (e.g. \code{person_UID}, \code{project_UID}).
}
