% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark-id.R
\name{mark_id}
\alias{mark_id}
\title{Mark identity columns for entity resolution}
\usage{
mark_id(df, cols, graph = "person", type, exact = TRUE)
}
\arguments{
\item{df}{A data frame.}

\item{cols}{One column, or several columns wrapped in \code{c()}, to mark.
Uses tidy evaluation, so bare column names work directly:
\code{mark_id(df, email, graph = "person", type = "email")} or
\code{mark_id(df, c(email_from_hr, email_from_irma), graph = "person", type = "email")}.}

\item{graph}{Name of the entity graph this identifier belongs to. Should
match a name previously passed to \code{\link[=init_id_envs]{init_id_envs()}}.}

\item{type}{A label for this kind of identifier (e.g. \code{"email"},
\code{"unikey"}, \code{"staff_id"}). Two values are only ever considered
candidates for the same identity if they share an \code{id_type}.}

\item{exact}{Currently unused; reserved for future fuzzy-matching
support.}
}
\value{
\code{df}, with \code{graph} and \code{id_type} attributes attached to the
marked column(s). No rows or columns are added, removed, or reordered.
}
\description{
Tags one or more columns of \code{df} with attributes recording which entity
graph they belong to (e.g. \code{"person"}, \code{"project"}) and what kind of
identifier they represent (e.g. \code{"email"}, \code{"unikey"}). These attributes
are read later by \code{\link[=add_to_identity_graph]{add_to_identity_graph()}} (to build the identity graph)
and \code{\link[=join_master_ids]{join_master_ids()}} (to attach a resolved master ID back onto \code{df}).
}
\examples{
df <- data.frame(email = c("a@example.com", "b@example.com"))
df <- mark_id(df, email, graph = "person", type = "email")
attr(df$email, "graph")
attr(df$email, "id_type")
}
